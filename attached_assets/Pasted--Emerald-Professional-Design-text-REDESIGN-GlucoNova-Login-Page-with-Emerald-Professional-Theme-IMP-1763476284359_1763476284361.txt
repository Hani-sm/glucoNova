 Emerald Professional Design
text
REDESIGN GlucoNova Login Page with Emerald Professional Theme

IMPLEMENT THIS EXACT DESIGN:

COLOR SCHEME:
- Background: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #0f172a 100%)
- Primary Color: #10b981 (Emerald Green)
- Secondary Color: #059669 (Dark Emerald)
- Accent Color: #06b6d4 (Teal)
- Text: #f8fafc (Pure White)
- Card Background: rgba(255, 255, 255, 0.08)
- Input Background: rgba(255, 255, 255, 0.05)

VISUAL ELEMENTS TO ADD:
1. Animated floating particles in background (subtle movement)
2. Medical waveform pattern (heartbeat or DNA strand) as overlay
3. Glassmorphism effects with backdrop blur
4. Smooth hover transitions and focus states

EXACT STYLING SPECIFICATIONS:

MAIN CONTAINER:
<div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 relative overflow-hidden">

BACKGROUND ANIMATIONS:
- Floating circular particles (emerald green, teal, white)
- Slow upward movement animation
- Medical waveform SVG pattern at 10% opacity

LOGIN CARD:
<div className="backdrop-blur-md bg-white/8 border border-white/15 rounded-2xl p-8 shadow-2xl">

FORM ELEMENTS:
Input Fields:
className="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 text-white placeholder-gray-300 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/20 transition-all duration-300"

Primary Button:
className="w-full bg-gradient-to-r from-emerald-600 to-emerald-500 hover:from-emerald-500 hover:to-emerald-400 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-[1.02] shadow-lg hover:shadow-emerald-500/25"

Checkbox:
className="rounded border-white/20 bg-white/5 text-emerald-500 focus:ring-emerald-500"

Links:
className="text-emerald-400 hover:text-emerald-300 transition-colors duration-200"

TYPOGRAPHY:
- "GlucoNova" header: text-3xl font-bold text-white
- "Login" subtitle: text-xl font-semibold text-emerald-400
- Body text: text-gray-200
- Footer: text-gray-400

SPECIFIC COMPONENT STRUCTURE:

1. Background Layer:
   - Gradient background
   - Animated floating particles
   - Medical waveform pattern

2. Login Card:
   - Glassmorphism container
   - Form with proper spacing
   - All interactive elements with emerald accents

3. Visual Hierarchy:
   - Clear typography scale
   - Consistent spacing (8px multiples)
   - Proper contrast ratios

ANIMATIONS TO IMPLEMENT:
- Floating particles: slow infinite float
- Input fields: smooth focus transitions
- Buttons: hover scale and glow effects
- Card: subtle fade-in on load

RESPONSIVE REQUIREMENTS:
- Mobile: Stacked layout, proper touch targets
- Tablet: Optimized spacing
- Desktop: Centered card with ample whitespace

ADD THESE EXACT ELEMENTS IN ORDER:
1. GlucoNova logo/header
2. Login title
3. Email input field
4. Password input field
5. Remember me checkbox
6. Sign in button
7. Forgot password link
8. Create account link
9. Copyright footer

PRIORITY: Make it look premium, medical, and trustworthy with the emerald green color scheme.
🔧 Additional CSS for Background Effects:
If Qoder needs the background animation code:

css
/* Floating particles animation */
@keyframes float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  50% { transform: translateY(-20px) rotate(180deg); }
}

.floating-particle {
  animation: float 6s ease-in-out infinite;
}

/* Medical waveform background */
.medical-pattern {
  background-image: url("data:image/svg+xml,...");
  opacity: 0.1;
}
This prompt will give you a sophisticated, professional healthcare login page with the Emerald Professional theme! The emerald green conveys trust and health while the dark background ensures excellent readability and modern appeal.